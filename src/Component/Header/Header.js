import React from 'react';
import H from './Header.module.css';

let Header = (props) => {
    return(
        <div className={H.header} >
                    HEADER
                </div>
    );
}

export default Header;